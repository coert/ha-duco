[tool.poetry]
name = "ha-duco"
version = "0.1.0"
description = ""
authors = ["coert <coert@alpha.one>"]
readme = "README.md"
packages = [{include = "ha_duco"}]

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
